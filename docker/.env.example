# Copy to `docker/.env` and fill in your own values.
# Do NOT commit `docker/.env` (it is covered by the repo's .gitignore).

# Ethereum JSON-RPC provider (recommended: your Alchemy Mainnet HTTPS URL)
ALCHEMY_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY_HERE
